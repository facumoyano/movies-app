import { Box } from "@chakra-ui/react";
import MoviesContainer from "../components/MoviesContainer";

const Home = () => {
    return (
        <Box>
            <MoviesContainer />
        </Box>
    );
};

export default Home;
